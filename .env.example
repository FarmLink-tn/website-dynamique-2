# Database configuration
DB_HOST=10.10.10.100
DB_PORT=3306
DB_NAME=brefzuoh_farmlink
# Replace with the MySQL database user that has privileges on brefzuoh_farmlink
DB_USER=
# Example: DB_PASSWORD=NtopQX-73y94+A (do not commit your production password)
DB_PASSWORD=
DB_CHARSET=utf8mb4

# Application
# When true, API responses include more verbose diagnostics (disable in production)
APP_DEBUG=false
SESSION_SECURE=false
SESSION_SAMESITE=Lax
